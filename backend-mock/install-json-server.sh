# Install NPM
sudo apt install npm -y

# Install YARN
sudo npm install --global yarn

# Install json-server and json-srever-auth
#sudo npm install -D json-server json-server-auth
npm install -g json-server

# Add json-server and json-server-auth to YARN
sudo yarn add -D json-server json-server-auth
